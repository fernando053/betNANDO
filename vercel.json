{
  "version": 2,
  "installCommand": "cd apps/api && npm install && npx prisma generate && cd ../web && npm install",
  "buildCommand": "cd apps/api && npm run build && cd ../web && npm run build",
  "outputDirectory": "apps/web/dist",
  "framework": null,
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/index.js" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/" }
  ],
  "functions": {
    "api/index.js": {
      "includeFiles": "apps/api/**",
      "maxDuration": 30
    }
  }
}
